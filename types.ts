export interface StudentResult {
  name: string;
  score: number;
  total: number;
}

export interface Feature {
  title: string;
  description?: string;
}

export interface NavItem {
  label: string;
  href: string;
}